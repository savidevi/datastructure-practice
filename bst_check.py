'''def isBst(self.root):
    def check(node,left,right):
        if node==None:
            return True
        if not(node.left<node and node.right>node):
            return false
        return check(node.left,left,node.data) and check(node.right,node.data,right)
    return check(root,float("-inf"),float("inf"))'''
'


